services:
  - type: worker
    name: overload-challenge-live-sync
    runtime: docker
    dockerfilePath: ./Dockerfile.worker
    envVars:
      - key: SUPABASE_DB_URL
        sync: false
      - key: CHALLENGE_SYNC_INTERVAL_MS
        value: "10000"
